spring:
  application:
    name: loan-platform

  datasource:
    url: jdbc:postgresql://localhost:5432/loan_platform
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
